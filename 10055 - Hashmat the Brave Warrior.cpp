#include <bits/stdc++.h>
#define pi 2.0*acos(0.0)
#define MAX 100005
typedef long long ll;
using namespace std;


int main()
{
    ll a,b,x;
    while(cin>>a>>b)
        {
            x=fabs(a-b);
            cout<<x<<endl;
        }
    return 0;
}
